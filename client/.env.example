# Leave empty for local dev — Vite's dev server proxies /api to http://localhost:5000
# (see vite.config.js). Only set this when deploying to production with a
# separately-hosted backend, e.g.:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
